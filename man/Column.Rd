% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Column.R
\name{Column}
\alias{Column}
\title{Define a database column for a model}
\usage{
Column(type, default = NULL, key = FALSE, nullable = TRUE, ...)
}
\arguments{
\item{type}{Character string representing the column's SQL data type (e.g. `"INTEGER"`, `"TEXT"`).}

\item{default}{The default value to use if none is supplied when creating a record. If `nullable = FALSE` and `default` is not provided, a basic default will be inferred from `type`. **Note:** currently not used when creating tables — for future support.}

\item{key}{Logical, whether this field is part of the primary key. **Note:** currently not used when creating tables — for future support.}

\item{nullable}{Logical, whether the field is allowed to be `NULL`. Defaults to `TRUE`. **Note:** currently not used when creating tables — for future support.}

\item{...}{Reserved for future dialect-specific extensions (e.g. `unique`, `collate`, `check`). Currently stored but **not used** during table creation.}
}
\value{
An object of class `"Column"`, used to define fields in a `BaseModel`.
}
\description{
Creates a `Column` object describing the type and constraints for a field
in a table model. Intended for use within `BaseModel` definitions.
}
\examples{
Column("TEXT", nullable = FALSE)
Column("INTEGER", key = TRUE, autoincrement = TRUE)

}
