% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Column.R, R/TableModel.R
\name{Column}
\alias{Column}
\title{Define a database column for a model}
\usage{
Column(
  type,
  default = NULL,
  primary_key = FALSE,
  nullable = TRUE,
  unique = FALSE,
  foreign_key = NULL,
  on_delete = NULL,
  on_update = NULL,
  ...
)

Column(
  type,
  default = NULL,
  primary_key = FALSE,
  nullable = TRUE,
  unique = FALSE,
  foreign_key = NULL,
  on_delete = NULL,
  on_update = NULL,
  ...
)
}
\arguments{
\item{type}{Character string representing the column's SQL data type
(e.g. `"INTEGER"`, `"TEXT"`, `"VARCHAR"`).}

\item{default}{The default value to use if none is supplied when creating a record.
If `NULL`, no default will be applied at the SQL level.}

\item{primary_key}{Logical. Whether this field is part of the primary key.
Defaults to `FALSE`.}

\item{nullable}{Logical. Whether the column may accept `NULL` values.
Defaults to `TRUE`.}

\item{unique}{Logical. Whether the column should be constrained as `UNIQUE`.
Defaults to `FALSE`. Currently unused in table creation.}

\item{foreign_key}{Optional character string specifying a foreign key reference
in the format `"referenced_table.referenced_column"`. Currently used for SQL generation.}

\item{on_delete}{Character string indicating `ON DELETE` behavior (e.g., `"CASCADE"`, `"SET NULL"`).
Only relevant if `foreign_key` is set. Optional.}

\item{on_update}{Character string indicating `ON UPDATE` behavior (e.g., `"CASCADE"`, `"RESTRICT"`).
Only relevant if `foreign_key` is set. Optional.}

\item{...}{Reserved for future extensions, such as `check`, `collate`, or custom constraints.}
}
\value{
An object of class `"Column"`, used to define fields in a TableModel.

An object of class `"Column"`, used to define fields in a TableModel.
}
\description{
Creates a `Column` object describing the type and constraints for a field
in a table model. Intended for use within `TableModel` or `BaseModel` definitions.

Creates a `Column` object describing the type and constraints for a field
in a table model. Intended for use within `TableModel` or `TableModel` definitions.
}
\examples{
Column("TEXT", nullable = FALSE)
Column("INTEGER", primary_key = TRUE)
Column("INTEGER", foreign_key = "users.id", on_delete = "CASCADE")

Column("TEXT", nullable = FALSE)
Column("INTEGER", primary_key = TRUE)
Column("INTEGER", foreign_key = "users.id", on_delete = "CASCADE")

}
