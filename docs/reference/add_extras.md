# Add extra SQL parts to column definition

This internal helper function adds additional SQL fragments from a list
of extras to the column definition.

## Usage

``` r
add_extras(parts, fields)
```

## Arguments

- parts:

  Character vector of existing SQL parts

- fields:

  List or vector of additional SQL fragments to add

## Value

Character vector with added SQL parts
