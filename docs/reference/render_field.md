# Render a column field to SQL

Render a column field to SQL

## Usage

``` r
render_field(field, conn, ...)
```

## Arguments

- field:

  A Column object

- conn:

  a DBI connection object

- ...:

  Ignored

## Value

A character SQL fragment
